1)
function min(number1, number2) {
if (number1 > number2)
return number2;
else if (number1 < number2)
return number1;
else
let findString = "Впиши число придурок";
return findString;
}
console.log(min(0, 10));
//
function min(number1, number2) {
if (number1 > number2)
return number2;
else  (number1 < number2)
return number1;
}
console.log(min(0, 10));

2)
function isEven(number) {
	if (number == 0){
    	return true;
    } 
  	else if ( number == 1 ){
    	return false;
    }
  	else if (number < 0) {
//    	const str ="минусовое число"
//    	return str;
//      return isEven(-number+2)
    }
  	else {
    return isEven(number-2);
    }
  }
  
  3)
//work
function countBs (testString) {
  	let stringLength = testString.length
    let countLetter = 0
    let i=0
    
	while (i<= stringLength) {
    let letter = testString.charAt(i)
    i++
    if (letter == "B") { 
      countLetter++
    	} 
    }
  return countLetter;
}
function countChar (testString, testChar ) {
  	let stringLength = testString.length
    let countLetter = 0
    let i=0
    
	while (i<= stringLength) {
    let letter = testString.charAt(i)
    i++
    if (letter == testChar) { 
      countLetter++
    	} 
    }
  return countLetter;
}
